::  p2 regression (DIVERGENCES row 21): a ~~ knot hex escape wider than 32
::  bits goes through ++tuft lane by lane, and rap drops the zero byte of an
::  empty lane (0x141 and 0x41 here); honk used to keep only the low lane
|%
++  main  [~~~100000041. ~~~4100000000.]
--
