::  c2 regression: one arm name twice in a single chapter. hoon-138's ++whap
::  replaces it with [%eror "duplicate arm: +x"] (hatch once kept the last
::  arm); ^+ only plays the core, so that arm lands in the artifact's type.
|%
++  main
  ^+  |%  ++  x  1  ++  x  2  --
  !!
--
