::  c2 regression: a duplicated arm with a postfix doc on each occurrence is
::  a bare [%eror "duplicate arm: +x"] in the artifact's type (++whap).
|%
++  main
  ^+  |%
      ++  x  1  ::  first
      ++  x  2  ::  second
      --
  !!
--
