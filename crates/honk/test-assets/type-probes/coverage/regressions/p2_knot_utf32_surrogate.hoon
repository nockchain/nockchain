::  p2 regression (DIVERGENCES row 21): ~-~d800. (a surrogate code point)
::  decodes to 0xd800, since ++taft only checks the round trip through
::  ++tuft; honk used to substitute 0xfffd
|%
++  main  ~-~d800.
--
