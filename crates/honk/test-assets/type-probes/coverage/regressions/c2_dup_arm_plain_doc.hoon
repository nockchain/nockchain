::  c2 regression: a duplicated arm under an unlinked (larg) prefix doc is a
::  bare [%eror "duplicate arm: +x"] in the artifact's type (++whap).
|%
++  main
  ^+  |%
      ::    plain doc
      ++  x  1
      ++  x  2
      --
  !!
--
