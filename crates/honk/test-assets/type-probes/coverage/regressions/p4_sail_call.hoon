::  p4 DIVERGENT (HOONC-ONLY): the sail `;%` (call) tuna mode. hoonc's
::  +tuna-mode has (cold %call cen); hatch runes/sail.rs tuna_tail (~162)
::  only knows `-` `+` `*`, so honk fails to parse.
|%
++  main  !>(..main)
++  sc
  ;div
    ;%  |=(a=marl:hoot a)
    ;p;
  ==
--
