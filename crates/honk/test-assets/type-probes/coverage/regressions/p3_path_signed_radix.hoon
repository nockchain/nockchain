::  regression (DIVERGENCES row 19): hoonc renders /-0x10 as '-0x10':
::  ++rend:co recurses on a signed dime with yed 'u' but keeps hay (hatch
::  rend_with_rep used to drop the radix and print '-16').
|%
++  main
  |=  a=@
  !>(/-0x10)
--
