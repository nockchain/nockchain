::  p4 DIVERGENT: tall `!?` with a version-range pair `[138 138]`. hoonc's
::  +hinh reads the pair as `dem ace dem`; hatch runes/zap.rs zapwut (~189)
::  demands a gap() between the numbers, so honk fails to parse.
|%
++  main  !>(..main)
++  a-zpwt2  !?  [138 138]  5
--
