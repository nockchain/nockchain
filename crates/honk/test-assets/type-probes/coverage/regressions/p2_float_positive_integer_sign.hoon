::  regression (DIVERGENCES row 12): a positive @rs literal with a
::  nonnegative decimal exponent; hatch binaryfloat_mul used to repeat the
::  zero test where ++mul:fl compares signs, flipping the result negative.
|%
++  main  .1
--
