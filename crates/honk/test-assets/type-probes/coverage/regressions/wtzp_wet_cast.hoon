::  a wet gate whose body is ?!(?=(~ a)), called with ~ and cast to %.n:
::  nesting expands the product hold by playing the arm at the call site.
|%
++  nonnull  |*  a=*  ?!(?=(~ a))
++  main
  |=  a=@
  [!>(^-(%.n (nonnull ~))) !>((nonnull a))]
--
