::  p2 regression: =/  =@  5  atom; hoonc autonames a bare @ as %atom.
::  variable_name_and_type relies on autoname, which once tested the aura
::  for "$" only while aura_text yields "" (and honk then crashed)
|%
++  main
  =/  =@  5
  atom
--
