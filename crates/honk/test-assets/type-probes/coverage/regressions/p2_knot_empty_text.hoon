::  p2 regression (DIVERGENCES row 21): ~~ and ~- alone are the empty @t
::  and @c, bare and as path segments (++urx is a star, so it may be empty);
::  honk's urx used to require a character
|%
++  main  [~~ ~- ~. /~~/~-]
--
