::  c2: ?= refinement through take_axis: a crashing arm's %hold that repos
::  to void (take_axis %void ~9516) and a battery test on a core-typed leg,
::  which repos the core to a cell (take_axis %core cap 2 ~9535-9537).
|%
++  bad  !!
++  main
  |=  b=?
  =+  a=bad
  =/  g  |=(c=@ c)
  :-  ?:(?=(@ -.a) !! !!)
  [!>(?:(?=(@ -.g) g g)) !>(?:(?=(^ -.g) g g))]
--
