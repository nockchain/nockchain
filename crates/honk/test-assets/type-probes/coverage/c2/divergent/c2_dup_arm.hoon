::  c2 divergent: one arm name twice in a single chapter. hoon-138's ++whap
::  replaces it with [%eror "duplicate arm: +x"]; hatch keeps the last arm,
::  so the arm hoon in the artifact's type differs.
|%
++  main
  ^+  |%  ++  x  1  ++  x  2  --
  !!
--
