::  p3: path segments re-rendered for signed decimal auras (rend_with_rep 's'
::  arm: syn_si both ways, abs_si, recursive 'u' render) and for @f/@n
|%
++  main
  |=  a=@
  :*  !>(/--1)
      !>(/-1)
      !>(/--0)
      !>(/--1.000)
      !>(/-1.000.000)
      !>(/.y)
      !>(/.n)
      !>(/~)
  ==
--
