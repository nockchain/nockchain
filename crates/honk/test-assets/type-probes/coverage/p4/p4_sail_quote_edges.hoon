::  p4: sail stored as arm bodies: `;p:''`, an empty cord (sail.rs
::  cord_bytes), and a `"""` block whose first line is empty (sail.rs
::  dedent_block).
|%
++  main  !>(..main)
++  c  ;p:''
++  q
  ;"""

   first
   """
--
