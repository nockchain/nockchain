::  p4 DIVERGENT: the `~$` (sgbc, profiler hit) rune. hoonc's +expression
::  `~` table has ['$' (rune buc %sgbc expf)]; hatch runes/sig.rs
::  sig_runes_tall/wide have no `$` branch, so honk fails to parse.
|%
++  main  !>(..main)
++  b1  ~$  %foo  5
--
