::  c6 divergent (HOONC-ONLY): hoonc ignores the /* mark and vases the file as
::  $octs; honk rejects every mark but %jam (pipeline.rs resolve_imports_for
::  ~239-249).
/*  blob  %txt  /data/blob/jam
`*`blob
