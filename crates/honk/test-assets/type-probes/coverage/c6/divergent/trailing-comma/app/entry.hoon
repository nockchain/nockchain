::  c6 divergent (HONK-ONLY): hoonc's `(most ;~(plug com gaw) taut-rule)`
::  needs an item after every comma; honk skips empty items
::  (pipeline.rs parse_import_clause ~553), so a trailing comma is accepted.
/-  *kinds,
`*`%a
