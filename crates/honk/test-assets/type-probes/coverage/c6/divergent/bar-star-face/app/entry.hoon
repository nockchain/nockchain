::  c6 divergent (HONK-ONLY): hoonc's /* rule requires a sym face; honk accepts
::  `*` and imports the data without a face (pipeline.rs
::  parse_bar_import_clause ~536).
/*  *  %jam  /data/blob/jam
`*`42
