::  c6 divergent (MISMATCH): hoonc evaluates /dat nodes eagerly and wraps the
::  value in its eval-vase trap; honk builds an unevaluated swet trap for every
::  /dat file except the pinned softed-constraints (honk.rs
::  compile_path_uncached ~2357-2380, native_value_override ~3258).
/#  const-data
`*`const-data
