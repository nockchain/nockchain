::  c6 (was divergence 30): hoonc's `(most ;~(plug com gaw) taut-rule)` takes
::  any whitespace and comments after a comma, so `base` on the next line is
::  imported.
/+  util,  ::  a comment after the comma
    base
`*`seed:base
