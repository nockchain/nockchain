::  c6: an eagerly evaluated /dat node that imports a library and whose
::  value is a gate over the standard library, so the value hoonc bakes in
::  carries the whole evaluated subject (hoon.hoon's core included).
/#  thing
`*`(thing 3)
