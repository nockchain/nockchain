::  c6 (was divergence 30): hoonc's +segments falls back to the literal name
::  `dbl--dash` when a hyphen-separated part is empty, so only
::  lib/dbl--dash.hoon is tried.
/+  dbl--dash
`*`dd:dbl--dash
