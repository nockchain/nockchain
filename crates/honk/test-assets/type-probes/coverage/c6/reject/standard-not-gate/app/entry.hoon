::  c6 rejection (both compilers): a standard (kernel) build slams the entry
::  with the @uvI directory hash (hoonc ++shot, honk.rs jam_product), and a
::  product that is not a gate cannot be slammed.
[%not %a %gate]
