::  c3: a gate minted against a forked core goal: the chapter-count check walks the goal fork
::  with type_fork_options/fork_set_options (mod.rs ~12376-12422).
|%
++  main
  |=  a=@
  =/  g  ^-(?($-(@ @) $-(@ ?)) |=(b=@ b))
  [!>(g) !>((g a))]
--
