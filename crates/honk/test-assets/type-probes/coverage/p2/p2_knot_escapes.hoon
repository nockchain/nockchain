::  p2: wick (utils.rs ~5840) on a bare `-` in a `._..__` element, and teff
::  (~6014) on a newline byte, in a `~-` (++taft) literal and while checking
::  a `~~` path knot for ++wood.
|%
++  main
  |=  a=@
  :*  !>(._-1__)
      !>(._--1_a-b__)
      !>(/._-1__)
      !>(~-~a.)
      !>(/~~~a.)
      !>(/~~a~a.b)
  ==
--
