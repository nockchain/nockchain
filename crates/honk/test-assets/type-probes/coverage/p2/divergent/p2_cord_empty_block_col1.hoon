::  p2 divergence: honk accepts an empty ''' block at column 1 that hoonc
::  rejects (++qut needs a content line before the closing newline);
::  cord() empty_triple_quoted utils.rs:5335-5338
|%
++  main
'''
'''
--
