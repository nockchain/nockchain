::  p2 divergence: rendering a float path segment >= 2^p (positive binary
::  exponent) differs: drg utils.rs:3563-3568 leaves mn/mp at 1 instead of
::  shifting them by the exponent like r (hoon-138 ++drg:fl)
|%
++  main  /.-1e30
--
