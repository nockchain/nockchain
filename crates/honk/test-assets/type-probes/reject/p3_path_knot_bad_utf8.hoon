::  p3: +hasp renders path knots with +scot, and ++wood crashes where ++taft
::  does: U+200000 is above U+10FFFF, so its UTF-8 bytes do not survive the
::  ++tuft round trip (utils.rs wood_crashes ~9116).
:-  /~~~200000.
~
