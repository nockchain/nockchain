::  A sail markdown rule leaves the input at the start of the next line,
::  so when the markdown ends there (an outdent) no gap is left before the
::  next arm (hoon-138 ++cram ++hrul, ++line).
|%
++  main
  ;>
    text

    ---
--
