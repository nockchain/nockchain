::  p2 rejection (DIVERGENCES row 21): ~-~1. puts a control byte through
::  ++taft, whose ++teff asserts every byte is printable or a newline, so
::  hoonc crashes; honk used to accept it as @c 1
|%
++  main  ~-~1.
--
