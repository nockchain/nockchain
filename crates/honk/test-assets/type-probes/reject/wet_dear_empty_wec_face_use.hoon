::  as wet_dear_empty_wec_atom, but the body names the sample: hoon-138 drops
::  the a= face (empty wec) so the vet-time ++mull cannot find a.
|%
++  wig
  |*  a=?(%foo %bar)
  a
++  main
  |=  [x=@ud b=?]
  !>((wig %baz))
--
